// ============================================================================
// globals.c - The single definition of the global state object.
// Every other module sees it via `extern GlobalState G;` in types.h.
// ============================================================================
#include "types.h"

GlobalState G;
